
<div class="container">
  {{#each themes}}
  <div class="puppy-list">
    <a href="/teams/{{id}}">
      <img class="img-circle" src="/images/{{image_url}}" alt="{{name}}">
    </a>
    <div>{{name}}</div>
  </div>
  {{/each}}
</div>